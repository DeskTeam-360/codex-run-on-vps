# Port proxy API (default: 8787)
PORT=8787

# Host bind — 0.0.0.0 agar bisa diakses dari luar VPS
HOST=0.0.0.0

# Path file auth Codex (hasil login ChatGPT Pro)
CODEX_AUTH_FILE=/root/.codex/auth.json
